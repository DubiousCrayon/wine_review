<% content_for :name, @wine.name %>
<h1><%= @wine.name %></h1>

<ul>
<li><label>Year: </label><%= @wine.year %></li>
<li><label>Winery: </label><%= @wine.winery %></li>
<li><label>Country: </label><%= @wine.country %></li>
<li><label>Varietal: </label><%= @wine.varietal %></li>
</ul>

<%= link_to("All Wines", wines_path) %> |
<%= link_to("Edit", edit_wine_path(@wine)) %> 

<%= button_to("Delete", @wine, method: :delete, data:{confirm: 'Are you sure?'}) %>